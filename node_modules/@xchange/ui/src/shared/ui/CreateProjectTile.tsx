import { Plus } from 'lucide-react'

type Props = {
  onClick: () => void
  label?: string
}

export function CreateProjectTile({ onClick, label = 'Create a new project' }: Props) {
  return (
    <button
      onClick={onClick}
      className="group w-full aspect-[4/5] min-h-[280px] rounded-2xl border-2 border-dashed border-accent/40 bg-accent/[0.03] hover:bg-accent/[0.06] hover:border-accent/70 transition-colors flex flex-col items-center justify-center gap-4"
    >
      <span className="w-14 h-14 rounded-full border border-foreground/40 flex items-center justify-center text-foreground/70 group-hover:border-accent group-hover:text-accent transition-colors">
        <Plus size={22} strokeWidth={1.5} />
      </span>
      <span className="text-base text-foreground/80 group-hover:text-foreground">{label}</span>
    </button>
  )
}
