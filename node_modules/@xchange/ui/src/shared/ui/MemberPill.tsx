import type { ObjectiveParticipant } from '@/entities/objective/types'

type Props = { member: ObjectiveParticipant }

export function MemberPill({ member }: Props) {
  return (
    <div className="flex items-center gap-1.5 bg-muted rounded-full px-2.5 py-1">
      <span className="h-4 w-4 rounded-full bg-accent/20 text-accent flex items-center justify-center text-xs font-medium">
        {member.displayName.charAt(0)}
      </span>
      <span className="text-xs">{member.displayName}</span>
    </div>
  )
}
