import type { ReactNode } from 'react'

type Props = {
  toolbar: ReactNode
  children: ReactNode
  className?: string
}

export function BoardLayout({ toolbar, children, className }: Props) {
  return (
    <div className={`flex flex-col h-full min-h-0 ${className ?? ''}`}>
      <div className="flex-shrink-0 px-6 py-4 border-b border-border bg-background">{toolbar}</div>
      <div className="flex-1 overflow-x-auto overflow-y-hidden">
        <div className="flex gap-4 p-6 h-full min-w-max">
          {children}
        </div>
      </div>
    </div>
  )
}
