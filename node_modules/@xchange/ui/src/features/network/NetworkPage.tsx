import { UserPlus, Check } from 'lucide-react'

const connections = [
  { id: 'u1', name: 'Ana Müller', role: 'Designer', mutual: 3, connected: true },
  { id: 'u2', name: 'Luis Schmidt', role: 'Engineer', mutual: 7, connected: true },
  { id: 'u3', name: 'Priya Patel', role: 'Product Manager', mutual: 2, connected: false },
  { id: 'u4', name: 'Tom Becker', role: 'Founder', mutual: 5, connected: false },
]

export function NetworkPage() {
  return (
    <div className="h-full overflow-y-auto p-6">
      <div className="max-w-3xl mx-auto">
        <h2 className="font-semibold mb-1">Network</h2>
        <p className="text-sm text-muted-foreground mb-6">Your professional connections in the ecosystem</p>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-3">
          {connections.map((u) => (
            <div key={u.id} className="flex items-center gap-3 bg-surface rounded-xl border border-border p-4">
              <div className="w-10 h-10 rounded-full bg-accent/10 text-accent flex items-center justify-center text-sm font-medium">
                {u.name.split(' ').map((n) => n[0]).join('')}
              </div>
              <div className="flex-1 min-w-0">
                <p className="text-sm font-medium truncate">{u.name}</p>
                <p className="text-xs text-muted-foreground">{u.role} · {u.mutual} mutual</p>
              </div>
              {u.connected ? (
                <span className="text-xs text-success flex items-center gap-1"><Check size={12} /> Connected</span>
              ) : (
                <button className="flex items-center gap-1 text-xs bg-accent text-accent-foreground px-2.5 py-1.5 rounded-lg hover:opacity-90">
                  <UserPlus size={12} /> Connect
                </button>
              )}
            </div>
          ))}
        </div>
      </div>
    </div>
  )
}
