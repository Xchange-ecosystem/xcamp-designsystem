import type { Objective } from '@/entities/objective/types'

type Props = { objective: Objective }

export function AboutTab({ objective }: Props) {
  return (
    <div className="p-6 max-w-2xl mx-auto">
      <div className="bg-surface rounded-xl border border-border p-6 flex flex-col gap-5">
        <div>
          <label className="text-xs text-muted-foreground font-medium uppercase tracking-wide block mb-1.5">Title</label>
          <input defaultValue={objective.title} className="w-full rounded-lg border border-border bg-background px-3 py-2 text-sm outline-none focus:border-accent" />
        </div>
        <div>
          <label className="text-xs text-muted-foreground font-medium uppercase tracking-wide block mb-1.5">Description / Statement of Work</label>
          <textarea defaultValue={objective.description} rows={5} className="w-full rounded-lg border border-border bg-background px-3 py-2 text-sm outline-none focus:border-accent resize-none" placeholder="Describe the objective, deliverables, and acceptance criteria..." />
        </div>
        <div className="grid grid-cols-2 gap-4">
          <div>
            <label className="text-xs text-muted-foreground font-medium uppercase tracking-wide block mb-1.5">Start date</label>
            <input type="date" defaultValue={objective.startDate} className="w-full rounded-lg border border-border bg-background px-3 py-2 text-sm outline-none focus:border-accent" />
          </div>
          <div>
            <label className="text-xs text-muted-foreground font-medium uppercase tracking-wide block mb-1.5">End date</label>
            <input type="date" defaultValue={objective.endDate} className="w-full rounded-lg border border-border bg-background px-3 py-2 text-sm outline-none focus:border-accent" />
          </div>
        </div>
        <div>
          <label className="text-xs text-muted-foreground font-medium uppercase tracking-wide block mb-1.5">Tags</label>
          <div className="flex flex-wrap gap-1.5">
            {objective.tags.map((tag) => (<span key={tag} className="px-2 py-0.5 rounded-full bg-muted text-xs">{tag}</span>))}
            <input placeholder="Add tag..." className="rounded-full border border-dashed border-border bg-transparent px-2 py-0.5 text-xs outline-none focus:border-accent w-20" />
          </div>
        </div>
        <div className="flex justify-end">
          <button className="bg-accent text-accent-foreground px-4 py-2 rounded-lg text-sm hover:opacity-90">Save changes</button>
        </div>
      </div>
    </div>
  )
}
