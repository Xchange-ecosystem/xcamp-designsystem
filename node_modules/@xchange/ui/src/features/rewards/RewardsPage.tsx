import { Gift, TrendingUp, Wallet, Clock } from 'lucide-react'

export function RewardsPage() {
  return (
    <div className="h-full overflow-y-auto p-6">
      <div className="max-w-3xl mx-auto">
        <h2 className="font-semibold mb-6">Rewards</h2>
        <div className="grid grid-cols-2 md:grid-cols-4 gap-4 mb-8">
          {[
            { label: 'Fund pool', value: '$12,000', icon: <Gift size={16} /> },
            { label: 'Distributed', value: '$3,500', icon: <TrendingUp size={16} /> },
            { label: 'In wallet', value: '$1,200', icon: <Wallet size={16} /> },
            { label: 'Pending', value: '$800', icon: <Clock size={16} /> },
          ].map((stat) => (
            <div key={stat.label} className="bg-surface rounded-xl border border-border p-4">
              <div className="flex items-center gap-2 text-accent mb-2">{stat.icon}</div>
              <p className="text-2xl font-bold">{stat.value}</p>
              <p className="text-xs text-muted-foreground mt-0.5">{stat.label}</p>
            </div>
          ))}
        </div>
        <div className="bg-surface rounded-xl border border-border p-6">
          <h3 className="font-medium mb-4">Issuance history</h3>
          <div className="flex flex-col gap-2">
            {[
              { label: 'Design system foundations', amount: '+$2,000', date: '2026-02-10', status: 'completed' },
              { label: 'Auth flows', amount: '+$1,500', date: '2026-03-01', status: 'pending' },
            ].map((entry) => (
              <div key={entry.label} className="flex items-center gap-3 py-2 border-b border-border last:border-0">
                <div className={`w-2 h-2 rounded-full ${entry.status === 'completed' ? 'bg-success' : 'bg-warning'}`} />
                <span className="text-sm flex-1">{entry.label}</span>
                <span className="text-xs text-muted-foreground">{entry.date}</span>
                <span className="text-sm font-medium text-success">{entry.amount}</span>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  )
}
