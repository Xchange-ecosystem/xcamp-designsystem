import { Award, ExternalLink } from 'lucide-react'

const contributions = [
  { id: 'c1', title: 'Designed onboarding flow', project: 'Build MVP Platform', impact: 'High', date: '2026-05-20' },
  { id: 'c2', title: 'Shipped community newsletter v1', project: 'Community Launch', impact: 'Medium', date: '2026-05-05' },
  { id: 'c3', title: 'Led research synthesis', project: 'Discovery Sprint', impact: 'High', date: '2026-04-22' },
]

export function PortfolioPage() {
  return (
    <div className="h-full overflow-y-auto p-6">
      <div className="max-w-3xl mx-auto">
        <h2 className="font-semibold mb-1">Portfolio</h2>
        <p className="text-sm text-muted-foreground mb-6">Showcase of your contributions across the ecosystem</p>
        <div className="grid grid-cols-3 gap-3 mb-6">
          {[{ l: 'Contributions', v: 18 }, { l: 'Projects', v: 6 }, { l: 'Endorsements', v: 12 }].map((s) => (
            <div key={s.l} className="bg-surface rounded-xl border border-border p-4 text-center">
              <p className="text-2xl font-bold">{s.v}</p>
              <p className="text-xs text-muted-foreground mt-1">{s.l}</p>
            </div>
          ))}
        </div>
        <div className="flex flex-col gap-2">
          {contributions.map((c) => (
            <div key={c.id} className="flex items-center gap-3 bg-surface rounded-xl border border-border p-4">
              <Award size={16} className="text-accent flex-shrink-0" />
              <div className="flex-1 min-w-0">
                <p className="text-sm font-medium">{c.title}</p>
                <p className="text-xs text-muted-foreground mt-0.5">{c.project} · {new Date(c.date).toLocaleDateString()}</p>
              </div>
              <span className={`text-xs px-2 py-0.5 rounded-full ${c.impact === 'High' ? 'bg-success/10 text-success' : 'bg-warning/10 text-warning'}`}>{c.impact}</span>
              <button className="text-muted-foreground hover:text-foreground"><ExternalLink size={14} /></button>
            </div>
          ))}
        </div>
      </div>
    </div>
  )
}
