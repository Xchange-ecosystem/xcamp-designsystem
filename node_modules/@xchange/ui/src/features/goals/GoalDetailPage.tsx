import { useParams, Link } from 'react-router-dom'
import { ArrowLeft, Target, Calendar, Users } from 'lucide-react'

const detail = {
  title: 'Ship MVP by Q1',
  description: 'Deliver a stable MVP including auth, projects, and objectives surface to invite-only users by end of March.',
  progress: 60,
  dueDate: '2026-03-31',
  owner: 'Ana Müller',
  linkedObjectives: [
    { id: 'obj-1', title: 'Auth & onboarding', status: 'completed' },
    { id: 'obj-2', title: 'Projects module', status: 'in_progress' },
    { id: 'obj-3', title: 'Objectives workspace', status: 'in_progress' },
  ],
}

export function GoalDetailPage() {
  const { projectId, goalId } = useParams()
  return (
    <div className="h-full overflow-y-auto p-6">
      <div className="max-w-3xl mx-auto">
        <Link to={`/app/project/${projectId}/goals`} className="text-xs text-muted-foreground hover:text-foreground flex items-center gap-1 mb-4">
          <ArrowLeft size={12} /> Back to goals
        </Link>
        <div className="flex items-center gap-2 mb-2">
          <Target size={18} className="text-accent" />
          <h2 className="font-semibold">{detail.title}</h2>
        </div>
        <p className="text-sm text-muted-foreground mb-6">{detail.description}</p>

        <div className="bg-surface rounded-xl border border-border p-5 mb-4">
          <div className="flex items-center justify-between mb-2">
            <span className="text-xs text-muted-foreground">Progress</span>
            <span className="text-sm font-medium">{detail.progress}%</span>
          </div>
          <div className="h-2 bg-muted rounded-full overflow-hidden">
            <div className="h-full bg-accent rounded-full" style={{ width: `${detail.progress}%` }} />
          </div>
          <div className="flex items-center gap-4 mt-4 text-xs text-muted-foreground">
            <span className="flex items-center gap-1"><Calendar size={12} /> Due {new Date(detail.dueDate).toLocaleDateString()}</span>
            <span className="flex items-center gap-1"><Users size={12} /> Owner: {detail.owner}</span>
          </div>
        </div>

        <div className="bg-surface rounded-xl border border-border">
          <div className="px-5 py-3 border-b border-border"><h3 className="font-medium text-sm">Linked objectives ({detail.linkedObjectives.length}) · #{goalId}</h3></div>
          <div className="divide-y divide-border">
            {detail.linkedObjectives.map((o) => (
              <Link key={o.id} to={`/app/objective/${o.id}`} className="flex items-center gap-3 px-5 py-3 hover:bg-muted/40">
                <span className="w-2 h-2 rounded-full bg-accent" />
                <span className="text-sm flex-1">{o.title}</span>
                <span className="text-xs text-muted-foreground capitalize">{o.status.replace('_', ' ')}</span>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  )
}
