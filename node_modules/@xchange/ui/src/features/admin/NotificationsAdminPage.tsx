const categories = ['invite', 'approval', 'assignment', 'evaluation', 'completion', 'billing', 'admin', 'ai', 'marketplace']

export function NotificationsAdminPage() {
  return (
    <div className="h-full overflow-y-auto p-6">
      <div className="max-w-3xl mx-auto">
        <h2 className="font-semibold mb-1">Notification defaults</h2>
        <p className="text-sm text-muted-foreground mb-6">Set default notification channels for new members</p>
        <div className="bg-surface rounded-xl border border-border overflow-hidden">
          <div className="grid grid-cols-[1fr_repeat(4,80px)] gap-2 px-4 py-2 text-xs text-muted-foreground border-b border-border">
            <span>Category</span><span className="text-center">In-app</span><span className="text-center">Email</span><span className="text-center">Push</span><span className="text-center">Digest</span>
          </div>
          {categories.map((c) => (
            <div key={c} className="grid grid-cols-[1fr_repeat(4,80px)] gap-2 px-4 py-3 text-sm border-b border-border last:border-b-0 items-center">
              <span className="capitalize">{c}</span>
              {[0, 1, 2, 3].map((i) => (
                <label key={i} className="flex justify-center">
                  <input type="checkbox" defaultChecked={i < 2} className="accent-accent" />
                </label>
              ))}
            </div>
          ))}
        </div>
      </div>
    </div>
  )
}
