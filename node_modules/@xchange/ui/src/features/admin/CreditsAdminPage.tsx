import { CreditCard, TrendingUp, Plus } from 'lucide-react'

const txs = [
  { id: 'a1', user: 'Ana Müller', type: 'allocation', amount: 500, date: '2026-05-28' },
  { id: 'a2', user: 'Luis Schmidt', type: 'top-up', amount: 1000, date: '2026-05-25' },
  { id: 'a3', user: 'Tom Becker', type: 'allocation', amount: 250, date: '2026-05-22' },
]

export function CreditsAdminPage() {
  return (
    <div className="h-full overflow-y-auto p-6">
      <div className="max-w-4xl mx-auto">
        <div className="flex items-center justify-between mb-6">
          <div>
            <h2 className="font-semibold">Credits</h2>
            <p className="text-sm text-muted-foreground">Manage tenant credit pool and allocations</p>
          </div>
          <button className="flex items-center gap-1.5 text-sm bg-accent text-accent-foreground px-3 py-1.5 rounded-lg hover:opacity-90">
            <Plus size={14} /> Top up tenant
          </button>
        </div>

        <div className="grid grid-cols-2 md:grid-cols-3 gap-3 mb-6">
          <div className="bg-surface rounded-xl border border-border p-4">
            <div className="flex items-center gap-2 text-muted-foreground text-xs"><CreditCard size={14} /> Pool</div>
            <p className="text-xl font-bold mt-2">12,500 XCR</p>
          </div>
          <div className="bg-surface rounded-xl border border-border p-4">
            <div className="flex items-center gap-2 text-muted-foreground text-xs">Allocated</div>
            <p className="text-xl font-bold mt-2">7,200 XCR</p>
          </div>
          <div className="bg-surface rounded-xl border border-border p-4">
            <div className="flex items-center gap-2 text-muted-foreground text-xs"><TrendingUp size={14} /> Burn rate</div>
            <p className="text-xl font-bold mt-2">420 / wk</p>
          </div>
        </div>

        <div className="bg-surface rounded-xl border border-border overflow-hidden">
          <div className="px-5 py-3 border-b border-border"><h3 className="font-medium text-sm">Recent transactions</h3></div>
          <div className="divide-y divide-border">
            {txs.map((t) => (
              <div key={t.id} className="flex items-center px-5 py-3">
                <div className="flex-1">
                  <p className="text-sm">{t.user}</p>
                  <p className="text-xs text-muted-foreground capitalize">{t.type} · {new Date(t.date).toLocaleDateString()}</p>
                </div>
                <span className="text-sm font-medium">{t.amount} XCR</span>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  )
}
