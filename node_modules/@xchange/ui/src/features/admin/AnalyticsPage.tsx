import { TrendingUp, Users, Activity, Target } from 'lucide-react'

const series = [12, 18, 14, 22, 28, 25, 32, 40, 38, 45, 52, 48]

export function AnalyticsPage() {
  const max = Math.max(...series)
  return (
    <div className="h-full overflow-y-auto p-6">
      <div className="max-w-4xl mx-auto">
        <h2 className="font-semibold mb-1">Analytics</h2>
        <p className="text-sm text-muted-foreground mb-6">Tenant-wide activity and growth</p>
        <div className="grid grid-cols-2 md:grid-cols-4 gap-3 mb-6">
          {[
            { l: 'Active users', v: '48', i: <Users size={14} /> },
            { l: 'Objectives', v: '124', i: <Target size={14} /> },
            { l: 'Completions', v: '67', i: <Activity size={14} /> },
            { l: 'MoM growth', v: '+18%', i: <TrendingUp size={14} /> },
          ].map((s) => (
            <div key={s.l} className="bg-surface rounded-xl border border-border p-4">
              <div className="flex items-center gap-1.5 text-muted-foreground text-xs">{s.i} {s.l}</div>
              <p className="text-xl font-semibold mt-1.5">{s.v}</p>
            </div>
          ))}
        </div>
        <div className="bg-surface rounded-xl border border-border p-5">
          <h3 className="text-sm font-medium mb-4">Activity (last 12 weeks)</h3>
          <div className="flex items-end gap-2 h-40">
            {series.map((v, i) => (
              <div key={i} className="flex-1 bg-accent/30 rounded-t hover:bg-accent transition-colors" style={{ height: `${(v / max) * 100}%` }} title={`${v}`} />
            ))}
          </div>
        </div>
      </div>
    </div>
  )
}
