import { FileText, ExternalLink } from 'lucide-react'

const docs = [
  { id: 'tos', name: 'Terms of Service', version: '2.1', updated: '2026-04-01' },
  { id: 'privacy', name: 'Privacy Policy', version: '1.7', updated: '2026-03-20' },
  { id: 'cookies', name: 'Cookie Policy', version: '1.2', updated: '2026-02-14' },
  { id: 'dpa', name: 'Data Processing Agreement', version: '1.0', updated: '2026-01-10' },
]

export function LegalPage() {
  return (
    <div className="h-full overflow-y-auto p-6">
      <div className="max-w-3xl mx-auto">
        <h2 className="font-semibold mb-1">Legal</h2>
        <p className="text-sm text-muted-foreground mb-6">Tenant-level legal documents</p>
        <div className="bg-surface rounded-xl border border-border divide-y divide-border">
          {docs.map((d) => (
            <div key={d.id} className="flex items-center gap-3 px-5 py-4">
              <FileText size={16} className="text-accent" />
              <div className="flex-1">
                <p className="text-sm font-medium">{d.name}</p>
                <p className="text-xs text-muted-foreground">v{d.version} · updated {new Date(d.updated).toLocaleDateString()}</p>
              </div>
              <button className="flex items-center gap-1 text-xs text-muted-foreground hover:text-foreground"><ExternalLink size={12} /> View</button>
            </div>
          ))}
        </div>
      </div>
    </div>
  )
}
