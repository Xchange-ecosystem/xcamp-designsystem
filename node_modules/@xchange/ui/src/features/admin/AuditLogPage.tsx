import { Shield, User } from 'lucide-react'

const entries = [
  { id: '1', actor: 'Ana Müller', action: 'updated tenant branding', when: '2026-05-28T10:00:00Z' },
  { id: '2', actor: 'system', action: 'rotated API key', when: '2026-05-27T22:13:00Z' },
  { id: '3', actor: 'Luis Schmidt', action: 'invited 3 members', when: '2026-05-27T15:40:00Z' },
  { id: '4', actor: 'Ana Müller', action: 'enabled app "rewards"', when: '2026-05-26T09:05:00Z' },
  { id: '5', actor: 'Tom Becker', action: 'created project "Community Launch"', when: '2026-05-25T14:00:00Z' },
]

export function AuditLogPage() {
  return (
    <div className="h-full overflow-y-auto p-6">
      <div className="max-w-3xl mx-auto">
        <h2 className="font-semibold mb-1">Audit log</h2>
        <p className="text-sm text-muted-foreground mb-6">Recent administrative activity</p>
        <div className="bg-surface rounded-xl border border-border divide-y divide-border">
          {entries.map((e) => (
            <div key={e.id} className="flex items-center gap-3 px-5 py-3">
              {e.actor === 'system' ? <Shield size={14} className="text-warning" /> : <User size={14} className="text-accent" />}
              <div className="flex-1 min-w-0">
                <p className="text-sm"><span className="font-medium">{e.actor}</span> <span className="text-muted-foreground">{e.action}</span></p>
              </div>
              <span className="text-xs text-muted-foreground whitespace-nowrap">{new Date(e.when).toLocaleString()}</span>
            </div>
          ))}
        </div>
      </div>
    </div>
  )
}
