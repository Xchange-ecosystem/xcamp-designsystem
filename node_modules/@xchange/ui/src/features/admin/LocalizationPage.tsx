import { Globe } from 'lucide-react'

const locales = [
  { code: 'en', label: 'English', coverage: 100, default: true },
  { code: 'de', label: 'Deutsch', coverage: 62, default: false },
  { code: 'es', label: 'Español', coverage: 58, default: false },
]

export function LocalizationPage() {
  return (
    <div className="h-full overflow-y-auto p-6">
      <div className="max-w-3xl mx-auto">
        <h2 className="font-semibold mb-1">Localization</h2>
        <p className="text-sm text-muted-foreground mb-6">Manage available languages and translation coverage</p>
        <div className="bg-surface rounded-xl border border-border overflow-hidden">
          {locales.map((l) => (
            <div key={l.code} className="flex items-center gap-3 px-5 py-4 border-b border-border last:border-b-0">
              <Globe size={16} className="text-accent" />
              <div className="flex-1">
                <p className="text-sm font-medium">{l.label} <span className="text-xs text-muted-foreground">({l.code})</span></p>
                <div className="flex items-center gap-2 mt-1">
                  <div className="flex-1 max-w-[200px] h-1.5 bg-muted rounded-full overflow-hidden">
                    <div className="h-full bg-accent" style={{ width: `${l.coverage}%` }} />
                  </div>
                  <span className="text-xs text-muted-foreground">{l.coverage}%</span>
                </div>
              </div>
              {l.default && <span className="text-xs px-2 py-0.5 rounded-full bg-accent/10 text-accent">Default</span>}
              <button className="text-xs px-3 py-1.5 rounded-lg border border-border hover:bg-muted">Manage</button>
            </div>
          ))}
        </div>
      </div>
    </div>
  )
}
