import { Mail, Edit3 } from 'lucide-react'

const templates = [
  { id: 'invite', name: 'User invitation', subject: 'You\'re invited to join {{tenant}}', updated: '2026-05-12' },
  { id: 'reset', name: 'Password reset', subject: 'Reset your password', updated: '2026-04-30' },
  { id: 'completion', name: 'Objective completed', subject: '{{objective}} is complete', updated: '2026-05-20' },
  { id: 'digest', name: 'Weekly digest', subject: 'Your week in {{tenant}}', updated: '2026-05-22' },
]

export function EmailTemplatesPage() {
  return (
    <div className="h-full overflow-y-auto p-6">
      <div className="max-w-3xl mx-auto">
        <h2 className="font-semibold mb-1">Email templates</h2>
        <p className="text-sm text-muted-foreground mb-6">Customize emails sent from your tenant</p>
        <div className="bg-surface rounded-xl border border-border divide-y divide-border">
          {templates.map((t) => (
            <div key={t.id} className="flex items-center gap-3 px-5 py-3">
              <Mail size={16} className="text-accent" />
              <div className="flex-1 min-w-0">
                <p className="text-sm font-medium">{t.name}</p>
                <p className="text-xs text-muted-foreground truncate">{t.subject}</p>
              </div>
              <span className="text-xs text-muted-foreground">{new Date(t.updated).toLocaleDateString()}</span>
              <button className="flex items-center gap-1 text-xs px-3 py-1.5 rounded-lg border border-border hover:bg-muted">
                <Edit3 size={12} /> Edit
              </button>
            </div>
          ))}
        </div>
      </div>
    </div>
  )
}
