import { useState } from 'react'
import { X, Plus } from 'lucide-react'

export function SkillsPage() {
  const [skills, setSkills] = useState(['Product Design', 'TypeScript', 'Facilitation'])
  const [interests, setInterests] = useState(['Web3', 'DAOs', 'Open source'])
  const [draft, setDraft] = useState('')

  const addTo = (which: 'skills' | 'interests') => {
    if (!draft.trim()) return
    if (which === 'skills') setSkills([...skills, draft.trim()])
    else setInterests([...interests, draft.trim()])
    setDraft('')
  }

  const TagList = ({ items, onRemove }: { items: string[]; onRemove: (i: number) => void }) => (
    <div className="flex flex-wrap gap-2">
      {items.map((s, i) => (
        <span key={s + i} className="inline-flex items-center gap-1 px-2.5 py-1 bg-muted rounded-full text-xs">
          {s}
          <button onClick={() => onRemove(i)} className="text-muted-foreground hover:text-foreground"><X size={10} /></button>
        </span>
      ))}
    </div>
  )

  return (
    <div className="h-full overflow-y-auto p-6">
      <div className="max-w-2xl mx-auto">
        <h2 className="font-semibold mb-1">Skills & interests</h2>
        <p className="text-sm text-muted-foreground mb-6">Help others discover you and improve matching</p>

        <div className="bg-surface rounded-xl border border-border p-5 mb-4">
          <h3 className="text-sm font-medium mb-3">Skills</h3>
          <TagList items={skills} onRemove={(i) => setSkills(skills.filter((_, idx) => idx !== i))} />
        </div>
        <div className="bg-surface rounded-xl border border-border p-5 mb-4">
          <h3 className="text-sm font-medium mb-3">Interests</h3>
          <TagList items={interests} onRemove={(i) => setInterests(interests.filter((_, idx) => idx !== i))} />
        </div>

        <div className="flex gap-2">
          <input value={draft} onChange={(e) => setDraft(e.target.value)} placeholder="Add a tag..." className="flex-1 rounded-lg border border-border bg-background px-3 py-2 text-sm outline-none focus:border-accent" />
          <button onClick={() => addTo('skills')} className="flex items-center gap-1 text-xs bg-accent text-accent-foreground px-3 py-2 rounded-lg hover:opacity-90"><Plus size={12} /> Skill</button>
          <button onClick={() => addTo('interests')} className="text-xs border border-border px-3 py-2 rounded-lg hover:bg-muted">+ Interest</button>
        </div>
      </div>
    </div>
  )
}
