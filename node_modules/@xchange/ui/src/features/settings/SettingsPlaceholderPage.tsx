type Props = { title: string }

export function SettingsPlaceholderPage({ title }: Props) {
  return (
    <div className="h-full overflow-y-auto p-6">
      <div className="max-w-2xl mx-auto">
        <h2 className="font-semibold mb-6">{title}</h2>
        <div className="bg-surface rounded-xl border border-dashed border-border p-10 text-center">
          <p className="text-sm text-muted-foreground">Settings for {title.toLowerCase()} will appear here.</p>
        </div>
      </div>
    </div>
  )
}
