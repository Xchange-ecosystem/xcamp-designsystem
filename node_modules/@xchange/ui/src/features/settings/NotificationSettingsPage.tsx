const categories = [
  { id: 'invite', label: 'Invitations' },
  { id: 'approval', label: 'Approvals' },
  { id: 'assignment', label: 'Assignments' },
  { id: 'evaluation', label: 'Evaluations' },
  { id: 'completion', label: 'Completions' },
  { id: 'billing', label: 'Billing' },
  { id: 'admin', label: 'Admin alerts' },
  { id: 'ai', label: 'AI suggestions' },
  { id: 'marketplace', label: 'Marketplace' },
]
const channels = ['inApp', 'email', 'push', 'digest'] as const

export function NotificationSettingsPage() {
  return (
    <div className="h-full overflow-y-auto p-6">
      <div className="max-w-3xl mx-auto">
        <h2 className="font-semibold mb-1">Notifications</h2>
        <p className="text-sm text-muted-foreground mb-6">Pick which channels deliver each category</p>
        <div className="bg-surface rounded-xl border border-border overflow-hidden">
          <div className="grid grid-cols-[1fr_repeat(4,90px)] gap-2 px-4 py-2 text-xs text-muted-foreground border-b border-border">
            <span></span>
            {channels.map((c) => <span key={c} className="text-center capitalize">{c}</span>)}
          </div>
          {categories.map((cat) => (
            <div key={cat.id} className="grid grid-cols-[1fr_repeat(4,90px)] gap-2 px-4 py-3 text-sm border-b border-border last:border-b-0 items-center">
              <span>{cat.label}</span>
              {channels.map((c) => (
                <label key={c} className="flex justify-center">
                  <input type="checkbox" defaultChecked={c === 'inApp' || c === 'email'} className="accent-accent" />
                </label>
              ))}
            </div>
          ))}
        </div>
      </div>
    </div>
  )
}
