import { useAuth } from '@/app/providers/AuthProvider'

export function AccountPage() {
  const { user } = useAuth() as any
  return (
    <div className="h-full overflow-y-auto p-6">
      <div className="max-w-2xl mx-auto">
        <h2 className="font-semibold mb-1">Account</h2>
        <p className="text-sm text-muted-foreground mb-6">Manage your profile information</p>
        <div className="bg-surface rounded-xl border border-border p-6 flex flex-col gap-4">
          <div>
            <label className="text-xs text-muted-foreground">Display name</label>
            <input defaultValue={user?.displayName ?? 'Demo user'} className="mt-1 w-full rounded-lg border border-border bg-background px-3 py-2 text-sm outline-none focus:border-accent" />
          </div>
          <div>
            <label className="text-xs text-muted-foreground">Email</label>
            <input defaultValue={user?.email ?? 'demo@xcamp.io'} className="mt-1 w-full rounded-lg border border-border bg-background px-3 py-2 text-sm outline-none focus:border-accent" />
          </div>
          <div>
            <label className="text-xs text-muted-foreground">Bio</label>
            <textarea rows={3} className="mt-1 w-full rounded-lg border border-border bg-background px-3 py-2 text-sm outline-none focus:border-accent resize-none" placeholder="A short bio..." />
          </div>
          <div className="flex justify-end">
            <button className="px-4 py-2 text-sm bg-accent text-accent-foreground rounded-lg hover:opacity-90">Save changes</button>
          </div>
        </div>
      </div>
    </div>
  )
}
