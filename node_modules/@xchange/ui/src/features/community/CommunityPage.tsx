import { Users, MessageCircle, Calendar } from 'lucide-react'

const groups = [
  { id: 'g1', name: 'Builders Circle', members: 124, topic: 'Product & engineering' },
  { id: 'g2', name: 'Design Crit', members: 87, topic: 'Weekly design feedback' },
  { id: 'g3', name: 'Founders Network', members: 56, topic: 'Founders supporting founders' },
]
const events = [
  { id: 'e1', title: 'Monthly Showcase', date: '2026-06-12' },
  { id: 'e2', title: 'AI x Web3 Roundtable', date: '2026-06-20' },
]

export function CommunityPage() {
  return (
    <div className="h-full overflow-y-auto p-6">
      <div className="max-w-3xl mx-auto">
        <h2 className="font-semibold mb-6">Community</h2>
        <h3 className="text-sm font-medium mb-3 flex items-center gap-2"><Users size={14} /> Groups</h3>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-3 mb-8">
          {groups.map((g) => (
            <div key={g.id} className="bg-surface rounded-xl border border-border p-4">
              <p className="font-medium text-sm">{g.name}</p>
              <p className="text-xs text-muted-foreground mt-1">{g.topic}</p>
              <p className="text-xs text-muted-foreground mt-2 flex items-center gap-3">
                <span className="flex items-center gap-1"><Users size={12} /> {g.members}</span>
                <span className="flex items-center gap-1"><MessageCircle size={12} /> Active</span>
              </p>
            </div>
          ))}
        </div>
        <h3 className="text-sm font-medium mb-3 flex items-center gap-2"><Calendar size={14} /> Upcoming events</h3>
        <div className="flex flex-col gap-2">
          {events.map((e) => (
            <div key={e.id} className="flex items-center justify-between bg-surface rounded-xl border border-border px-4 py-3">
              <span className="text-sm">{e.title}</span>
              <span className="text-xs text-muted-foreground">{new Date(e.date).toLocaleDateString()}</span>
            </div>
          ))}
        </div>
      </div>
    </div>
  )
}
